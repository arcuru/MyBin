This repo is mostly comprised of very old code, school projects, and small scripts that don't really need their own repo.

It is mostly used as a bundle of existing code that I can test out some new stuff that I'm learning (e.g. adding CMake files)
